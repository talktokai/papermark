/**
 * Read-only rendering of how a link preview card will look.
 *
 * Stub replacement: the upstream repository imports this but does not publish
 * it. It is presentational only — nothing is lost besides the preview itself.
 */
export function BrandingSocialPreviewReadonly(_props: {
  title: string | null;
  description: string | null;
  image: string | null;
  favicon: string | null;
}) {
  return null;
}

export default BrandingSocialPreviewReadonly;
