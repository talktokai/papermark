/**
 * Browser-chrome frame wrapping the live branding preview iframe.
 *
 * Stub replacement: the upstream repository imports this but does not publish
 * it. Rendering nothing hides the preview pane; the branding settings
 * themselves still save and apply.
 */
export function BrandingPreviewChrome(_props: {
  name: string;
  basePath: string;
  urlLabel?: string;
  params?: Record<string, string>;
}) {
  return null;
}

export default BrandingPreviewChrome;
