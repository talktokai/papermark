/**
 * Toolbar entry point for starting a document redaction.
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. Rendering nothing removes the redaction action from the
 * document header.
 */
export function RedactionLauncher(_props: {
  documentId: string;
  documentName: string;
  documentType?: string | null;
}) {
  return null;
}

export default RedactionLauncher;
