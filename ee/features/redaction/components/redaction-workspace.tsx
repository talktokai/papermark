/**
 * Full-page workspace for reviewing and applying redactions.
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. The /documents/[id]/redactions/[jobId] route renders an
 * empty page; nothing links to it once the launcher is stubbed out.
 */
export function RedactionWorkspace(_props: {
  documentId: string;
  documentName: string;
  jobId: string;
}) {
  return null;
}

export default RedactionWorkspace;
