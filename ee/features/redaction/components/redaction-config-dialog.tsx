/**
 * Dialog configuring a new redaction run.
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. It is loaded via next/dynamic, so the named export must
 * still exist for the chunk to resolve.
 */
export function RedactionConfigDialog(_props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  documentId: string;
  documentName: string;
}) {
  return null;
}

export default RedactionConfigDialog;
