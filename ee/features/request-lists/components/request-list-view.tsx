/**
 * Dashboard page body for a dataroom's request list (tasks).
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. The /datarooms/[id]/tasks route renders nothing; the
 * feature gate hides the navigation entry that leads there.
 */
export function RequestListView(_props: { dataroomId: string }) {
  return null;
}

export default RequestListView;
