/**
 * Dataroom settings card toggling the request-list feature.
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. Props mirror the call site in
 * pages/datarooms/[id]/settings/index.tsx.
 */
export function RequestListSettingsCard(_props: {
  dataroomId: string;
  teamId: string;
  requestListEnabled: boolean;
}) {
  return null;
}

export default RequestListSettingsCard;
