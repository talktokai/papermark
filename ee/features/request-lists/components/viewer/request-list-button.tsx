/**
 * Viewer toolbar button opening the request-list sheet.
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. Callers only render it when `useViewerRequestList` reports
 * the feature enabled, which the stubbed hook never does.
 */
export function RequestListButton(_props: { className?: string }) {
  return null;
}

export default RequestListButton;
