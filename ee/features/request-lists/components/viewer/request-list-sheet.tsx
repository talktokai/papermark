/**
 * Viewer-facing sheet listing the tasks requested from them.
 *
 * Stub replacement: the upstream repository imports this component but does
 * not publish it. Props mirror the call site in
 * components/view/dataroom/nav-dataroom.tsx.
 */
export function RequestListSheet(_props: {
  linkId: string;
  dataroomId: string;
  viewId: string;
  viewerId?: string;
  isOpen: boolean;
  onOpenChange: (open: boolean) => void;
}) {
  return null;
}

export default RequestListSheet;
