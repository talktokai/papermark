/**
 * Cross-component event name used to toggle the viewer request-list sheet.
 *
 * Stub replacement: the upstream repository imports this constant but does
 * not publish it. Only the string identity matters — the dispatcher and the
 * listener both read it from here.
 */
export const VIEWER_TOGGLE_REQUEST_LIST_EVENT = "viewer:toggle-request-list";
